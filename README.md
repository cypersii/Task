# NodeJS API REST with MongoDB Database 
This task explains how to:
* create simple REST APIs with express.
* create some unit tests with mocha.
* Store data into MongoDB Database

To run the server:
* npm start